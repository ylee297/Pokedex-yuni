import classes from "../styles/Topbar.module.css"

const Topbar = () => {
    return (
        <div className={classes.container}>
            <p>Pokedex</p>  
        </div>
    );
};

export default Topbar;